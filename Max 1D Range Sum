#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
int main()
{
int n=8,sum=0,max_sum=0;
int x[n]={1,-3,-2,5,4,-1,-5,1};
for(int i=0;i<n;i++)
{
    sum+=x[i];
    if(sum<0)sum=0;
    max_sum=max(max_sum,sum);
}
cout<<max_sum;

        return 0;
}
